import "@/styles/waiter.css";

import { WaiterLayout } from "@/components/waiter/waiter-layout";
export default function Layout({ children }: { children: React.ReactNode }) { return <WaiterLayout>{children}</WaiterLayout>; }
