"use client";
import { useState } from "react";
const methods=["Cash","Card","eSewa","Khalti","Bank Transfer","Other"];
export default function BillingPage(){const [method,setMethod]=useState("Cash");const [amount,setAmount]=useState("730");const total=730;const paid=Number(amount)||0;return <><h1 className="page-title">Billing · Order #1042</h1><div className="grid" style={{gridTemplateColumns:"1.2fr .8fr",marginTop:20}}><article className="card"><h2 style={{marginTop:0}}>Bill items</h2><table className="table"><tbody><tr><td>Chicken Momo × 2</td><td>NPR 520</td></tr><tr><td>Cappuccino × 1</td><td>NPR 190</td></tr></tbody></table><div className="total"><span>Subtotal</span><span>NPR 710</span></div><div className="total"><span>Tax</span><span>NPR 20</span></div><div className="total"><strong>Total</strong><strong>NPR {total}</strong></div></article><aside className="card"><h2 style={{marginTop:0}}>Payment</h2><div className="tabs">{methods.map(item=><button key={item} className={`tab ${method===item?"active":""}`} onClick={()=>setMethod(item)}>{item}</button>)}</div><label className="field">Amount<input type="number" value={amount} onChange={e=>setAmount(e.target.value)}/></label>{method!=="Cash"&&<label className="field">Reference number<input placeholder="Transaction reference"/></label>}<p className="muted">Paid: NPR {paid} · Remaining: NPR {Math.max(total-paid,0)}</p>{method==="Cash"&&<p className="muted">Change: NPR {Math.max(paid-total,0)}</p>}<button className="btn" style={{width:"100%"}} disabled={paid<=0}>Confirm payment</button></aside></div></>}
